apiVersion: v1
kind: BuildJob
metadata:
  name: test-build
  namespace: test

stages:
  build:
    script:
      - |
        echo building...
        ls -la
        echo still building...
        sleep 2
      - echo next...

  deploy:
    script: 
      - |
        echo deploying...

    